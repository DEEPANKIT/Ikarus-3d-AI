# Ikarus 3D Product Recommendation System - Full Dependencies
# Python 3.12 compatible - builds on working minimal setup

# =============================================================================
# CORE WEB FRAMEWORK (WORKING)
# =============================================================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# =============================================================================
# DATA PROCESSING (WORKING)
# =============================================================================
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# =============================================================================
# MACHINE LEARNING (ADD GRADUALLY)
# =============================================================================
scikit-learn>=1.3.0
# torch>=2.0.0  # Install separately if needed
# torchvision>=0.15.0  # Install separately if needed
# transformers>=4.30.0  # Install separately if needed
# sentence-transformers>=2.2.0  # Install separately if needed

# =============================================================================
# NLP (ADD GRADUALLY)
# =============================================================================
# spacy>=3.6.0  # Install separately if needed
# nltk>=3.8.0  # Install separately if needed

# =============================================================================
# COMPUTER VISION (ADD GRADUALLY)
# =============================================================================
# opencv-python>=4.8.0  # Install separately if needed
# Pillow>=10.0.0  # Install separately if needed

# =============================================================================
# VECTOR DATABASE (ADD GRADUALLY)
# =============================================================================
# pinecone-client>=2.2.0  # Install separately if needed
# faiss-cpu>=1.7.0  # Install separately if needed

# =============================================================================
# DATABASE & CACHING (ADD GRADUALLY)
# =============================================================================
# sqlalchemy>=2.0.0  # Install separately if needed
# redis>=5.0.0  # Install separately if needed

# =============================================================================
# HTTP & ASYNC (WORKING)
# =============================================================================
httpx==0.25.2
aiofiles>=23.0.0

# =============================================================================
# ENVIRONMENT & CONFIGURATION (WORKING)
# =============================================================================
python-dotenv==1.0.0
# python-jose[cryptography]>=3.3.0  # Install separately if needed
# passlib[bcrypt]>=1.7.0  # Install separately if needed

# =============================================================================
# TESTING (WORKING)
# =============================================================================
pytest==7.4.3
pytest-asyncio>=0.21.0

# =============================================================================
# DEVELOPMENT (ADD GRADUALLY)
# =============================================================================
# jupyter>=1.0.0  # Install separately if needed
# matplotlib>=3.7.0  # Install separately if needed
# seaborn>=0.12.0  # Install separately if needed

# =============================================================================
# PRODUCTION (ADD GRADUALLY)
# =============================================================================
# gunicorn>=21.0.0  # Install separately if needed



